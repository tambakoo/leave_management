<center>
  <% @users = User.all.map {|u|[u.email, u.id]} %>
  <% @states = Leave.states.keys << "all"  %>
  <%= form_tag employee_records_path, :method => :get do %>
    <%= select_tag("user_id", options_for_select(@users)) %>
    <%= select_tag("state", options_for_select(@states)) %>
    <%= submit_tag 'Filter' %>
<% end %>
<br>
<%= render 'record_table'%>
 <br>
</center>  
